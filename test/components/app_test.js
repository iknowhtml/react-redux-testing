import {renderComponent, expect} from '../test_helper';
import App from '../../src/components/app';

// use describe to group together similar tests
// specify string inside describe for testing output report
describe('App')

// use 'it' to test a single attribute of a target
// use 'it' to group one single test
// used ONLY for the testing output report
it('shows the correct text')

// use expect to make an 'assertion' about a target or what you are testing
expect()
